# EnaPlanner 可选骰子系统：调查与设计计划

## 目标

在修改功能代码前，梳理 LittleWhiteBox 中 EnaPlanner 的模块链、模板保存、配置保存以及规划/正文兜底发送流程，形成经用户确认的骰子系统设计。

## 约束

- 用户确认设计前不修改功能代码。
- 骰子系统应可开关、可排序、可编辑保存，并在同轮规划与空回正文兜底间复用解析后的骰子结果。
- 现有模块行为不得受到影响。

## 阶段

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| 1. 恢复上下文与定位仓库 | complete | 已确认项目为 `LittleWhiteBox` 独立仓库，尚无已有计划文件。 |
| 2. 调查现有实现 | complete | 已核对模块链、设置、模板/配置持久化、发送/空回、宏解析与相关回退历史。 |
| 3. 澄清设计边界 | complete | 已确认默认内容、模块保护模式和空回给正文的承载方式。 |
| 4. 方案比较与设计确认 | complete | 用户已批准推荐方案、运行时兜底与验证边界。 |
| 5. 写入批准后的设计规范 | complete | spec 已完成自查并以提交 `f9c2810` 单独提交，用户已审阅批准。 |
| 6. 实施计划 | complete | 已依照批准后的 spec 写入测试先行实施计划，并完成覆盖面与占位内容自查。 |

## 错误记录

| 错误 | 尝试 | 处理 |
| --- | --- | --- |
| 容器目录不是 Git 仓库 | 在 `d:\Github_All\stx相关` 查询 git 状态 | 转到实际项目根目录 `LittleWhiteBox` 调查。 |
| Spec 暂存校验报告三处行尾空格 | `git diff --cached --check` | 清理日期行及默认提示词中的尾随空格后重新校验。 |

## 已确认的产品选择

- 骰子模块默认内容使用用户提供的 `<合理性审查>` 与包含 10 个 `D20` 的 `<骰子池>` 文本。
- 骰子模块始终保留在统一模块链内，可编辑且可排序，但不可删除、不可通过模块卡片单独关闭；是否执行仅由基本设置中的独立总开关决定。
- 当规划过滤结果为空时，将本轮已经解析过宏的骰子规则文本追加到原始玩家输入后，再沿用现有发送流程交给正文模型；禁止重新执行骰子宏。
- 采用推荐实现路径：固定 ID 的受保护 `promptBlock` 进入现有 `promptBlocks/moduleChain`，由单轮上下文在模块遍历位置解析一次骰子宏并承担空回复用。
- 配置/链设计已确认：`diceSystem.enabled` 为全局开关；骰子块固定为 `system`、可编辑可排序但不可删除或单独关闭；模板携带块内容及位置但不携带总开关；旧配置/模板缺块时自动补入且不覆盖已有编辑或排序。
- 运行时失败策略已确认：规划消息构造先解析骰子模块，随后才调用规划 API；因此规划 API 空响应、抛错或超时时已有同轮骰池，均走已解析骰池的正文兜底，而不是恢复原输入并终止。
- 正文请求已经由放行发送提交后的失败不由 EnaPlanner 自动重发，以避免重复正文生成。

## 设计规范

- 文件：`docs/superpowers/specs/2026-05-24-ena-planner-dice-system-design.md`
- 提交：`f9c2810` (`Document EnaPlanner dice system design`)
- 状态：用户已批准；进入实施计划阶段。

## 实施计划

- 文件：`docs/superpowers/plans/2026-05-24-ena-planner-dice-system.md`
- 状态：用户选择 `Inline Execution`，允许在当前仓库直接实现且将工作记录纳入后续提交。
- 执行方式：按 TDD 顺序先建立失败测试，再接入功能实现与验证。
